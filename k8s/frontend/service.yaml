apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: frontend
    type: devops-frontend
  ports:
    - name: http
      protocol: TCP
      port: 5000
      targetPort: 5000