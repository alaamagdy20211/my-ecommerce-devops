apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: backend
    type: devops-backend
  ports:
    - name: http
      protocol: TCP
      port: 5000
      targetPort: 5000